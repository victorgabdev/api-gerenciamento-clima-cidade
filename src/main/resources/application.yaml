server:
  port: 8089

spring:
  ## APPLICATION ##
  application:
    name: api-genciamento-clima-cidade

  ## DATABASE ##
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: victor
    password: pass

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true



